# Pinned: each tagged release's CI build runs a fresh `pip install`, so an
# unpinned dep here can silently change behavior between releases with no
# corresponding code change (e.g. a newer pymupdf4llm changed its OCR
# page-metadata shape and broke PDF parsing on the v0.1.6 desktop build).
# Bump deliberately, and re-verify a real PDF conversion end-to-end after.
flask==3.1.3
pymupdf==1.26.5
pymupdf4llm==0.0.27
anthropic==0.121.0
latex2mathml==3.78.1
markdown==3.9
python-dotenv==1.2.1
